"""Model windows specifying where terms operate over wavelength or fibers.

Google-style docstrings are used throughout.
"""
from __future__ import annotations

from typing import List, Tuple


def full_band() -> List[Tuple[float, float]]:
    """Return a single window covering the full modeled range (placeholder).

    Returns:
        List of one (start, stop) tuple.
    """
    return []
