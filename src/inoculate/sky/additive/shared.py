"""Shared additive model constraints across the 3 exposures (placeholder).

Google-style docstrings are used throughout.
"""
from __future__ import annotations

from typing import Any, Dict


def tie_across_exposures(polys: Any) -> Dict[str, Any]:
    """Tie additive polynomial parameters across exposures (placeholder).

    Args:
        polys: Collection of per-exposure polynomial parameters.

    Returns:
        A mapping with tied parameters or constraints.
    """
    return {}
