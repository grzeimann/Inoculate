"""Additive polynomial models for amplifier excess charge (placeholder).

Google-style docstrings are used throughout.
"""
from __future__ import annotations

from typing import Any, Dict


def fit_additive_poly(spectrum: Any, order: int = 3) -> Dict[str, Any]:
    """Fit an additive polynomial to a spectrum (placeholder).

    Args:
        spectrum: 1D array-like spectrum for a single amplifier.
        order: Polynomial order.

    Returns:
        Mapping with polynomial coefficients and diagnostics.
    """
    return {}
