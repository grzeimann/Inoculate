"""Build PCA bases for sky residuals (placeholder)."""
from __future__ import annotations

from typing import Any, Dict


def build_pca_basis(data: Any, n_components: int = 8) -> Dict[str, Any]:
    """Construct a PCA basis from input spectra (placeholder).

    Args:
        data: Input bundle of spectra and weights.
        n_components: Number of PCA components to retain.

    Returns:
        Mapping with basis vectors and metadata.
    """
    return {}
