"""Apply multiplicative factors to spectra (placeholder).

Google-style docstrings are used throughout.
"""
from __future__ import annotations

from typing import Any


def apply_multiplicative(spectrum: Any, factor: Any) -> Any:
    """Apply a multiplicative factor to a spectrum (placeholder).

    Args:
        spectrum: 1D array-like spectrum.
        factor: Multiplicative factor array-like broadcastable to ``spectrum``.

    Returns:
        The scaled spectrum.
    """
    raise NotImplementedError("apply_multiplicative not yet implemented.")
